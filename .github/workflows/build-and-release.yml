name: Build and Release

on:
  push:
    branches:
      - master

# 添加权限配置
permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Gradle Wrapper
        run: |
          if [ ! -f gradlew ]; then
            echo "gradlew not found, generating..."
            gradle wrapper
          else
            echo "gradlew found"
          fi

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Get version and timestamp
        id: vars
        run: |
          VERSION=$(grep '^mod_version=' gradle.properties | cut -d'=' -f2)
          TIMESTAMP=$(date +'%Y%m%d-%H%M%S')
          TAG="${VERSION}-${TIMESTAMP}"
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Find built JAR
        id: find_jar
        run: |
          JAR_FILE=$(find build/libs -name "*.jar" ! -name "*-sources.jar" ! -name "*-dev.jar" | head -n 1)
          if [ -z "$JAR_FILE" ]; then
            echo "No JAR file found!"
            exit 1
          fi
          echo "jar_path=${JAR_FILE}" >> $GITHUB_OUTPUT
          echo "jar_name=$(basename ${JAR_FILE})" >> $GITHUB_OUTPUT

      # 检查 commit 标题是否以 release 开头（不区分大小写）
      - name: Check commit title
        id: check_release
        run: |
          COMMIT_MSG=$(git log -1 --pretty=format:"%s")
          echo "Commit message: $COMMIT_MSG"
          if echo "$COMMIT_MSG" | grep -qi "^release"; then
            echo "is_release=true" >> $GITHUB_OUTPUT
            echo "✅ Commit starts with 'release' - will create Release"
          else
            echo "is_release=false" >> $GITHUB_OUTPUT
            echo "⏭️ Commit does not start with 'release' - skipping Release"
          fi

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        if: steps.check_release.outputs.is_release == 'true'
        with:
          tag_name: ${{ steps.vars.outputs.tag }}
          name: "Release ${{ steps.vars.outputs.tag }}"
          body: |
            ## Auto-generated Release
            - Version: ${{ steps.vars.outputs.version }}
            - Build Time: ${{ github.event.head_commit.timestamp }}
            - Commit: ${{ github.sha }}
          draft: false
          prerelease: false
          files: |
            ${{ steps.find_jar.outputs.jar_path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
